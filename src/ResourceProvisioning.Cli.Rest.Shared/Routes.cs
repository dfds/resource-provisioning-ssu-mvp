namespace ResourceProvisioning.Cli.RestShared
{
    public static class Routes
    {
        public const string STATE = "state";
        public const string STATE_DASH_ENVIRONMENT_ID = STATE + "/{environmentId}";
    }
}
